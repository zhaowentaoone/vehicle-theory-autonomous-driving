# 第 04 章案例　加速与制动

默认：驱动力上限 4000 N，制动减速度目标约 0.7g（受 $F_x$ 下限限制）。先加速到 28 m/s，再全力制动。

```bash
python "第04章-纵向动力学/04-案例/python/longitudinal_sim.py"
```

**做对的标志**：速度先升后降到 0；制动段近似直线减速；打印 0–100 km/h 时间约 10–13 s（有风阻），制动距离约 50–70 m。
